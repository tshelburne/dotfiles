# Modern global npm packages
# Install with: cat setup/packages/npm.txt | grep -v '^#' | xargs npm install -g

# Git diff tool
diff-so-fancy

# Package manager (faster alternative to npm)
pnpm

# TypeScript tooling (if needed)
# typescript
# ts-node

# Linting/Formatting (if needed)
# eslint
# prettier
